extends layout

block content
  body
  div(class="container text-center col-md-4 col-md-offset-4")
    h1 內鬼粗來玩
    ul(class="list-group")
      each user,index in list
        li(class="list-group-item") 內鬼#{index}： #{user}